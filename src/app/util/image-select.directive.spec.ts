import { ImageSelectDirective } from './image-select.directive';

describe('ImageSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSelectDirective();
    expect(directive).toBeTruthy();
  });
});
